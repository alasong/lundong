[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
timeout = 30
addopts =
    -v
    --tb=short
    -ra
    -q
markers =
    e2e: End-to-end tests (slow, skip in pre-push)
    integration: Integration tests
    slow: Slow running tests
